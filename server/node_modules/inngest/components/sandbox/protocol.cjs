const require_rolldown_runtime = require('../../_virtual/rolldown_runtime.cjs');
const require_types = require('./types.cjs');
const require_validation = require('./validation.cjs');
let zod_v3 = require("zod/v3");

//#region src/components/sandbox/protocol.ts
const sandboxTargetSchema = zod_v3.z.object({ sandbox: require_validation.sandboxRefSchema }).strict();
const processIdTargetSchema = sandboxTargetSchema.extend({ processId: require_validation.canonicalUuidSchema }).strict();
const processTargetSchema = sandboxTargetSchema.extend({ process: require_validation.sandboxProcessRefSchema }).strict().superRefine((target, ctx) => {
	if (target.process.sandboxId !== target.sandbox.id) ctx.addIssue({
		code: zod_v3.z.ZodIssueCode.custom,
		message: "sandbox process reference belongs to another sandbox"
	});
});
const createInputSchema = zod_v3.z.object({
	name: require_validation.sandboxNameSchema,
	vcpu: zod_v3.z.number().int().positive().max(4294967295),
	memoryMb: zod_v3.z.number().int().positive().max(4294967295),
	environment: zod_v3.z.record(zod_v3.z.string()).optional(),
	runningTimeoutMs: zod_v3.z.number().int().positive().max(3e5).optional()
}).strict().superRefine((value, ctx) => {
	try {
		const { runningTimeoutMs, ...options } = value;
		require_validation.normalizeSandboxCreateOptions({
			...options,
			...runningTimeoutMs !== void 0 && { runningTimeout: runningTimeoutMs }
		});
	} catch (error) {
		ctx.addIssue({
			code: zod_v3.z.ZodIssueCode.custom,
			message: error instanceof Error ? error.message : "invalid create options"
		});
	}
});
const listInputSchema = zod_v3.z.object({
	cursor: zod_v3.z.string().min(1).optional(),
	limit: zod_v3.z.number().int().min(1).max(250)
}).strict();
const getInputSchema = zod_v3.z.object({ sandboxId: require_validation.canonicalUuidSchema }).strict();
const processSpecShape = {
	command: zod_v3.z.array(zod_v3.z.string()).min(1).max(128),
	environment: zod_v3.z.record(zod_v3.z.string()).optional(),
	cwd: zod_v3.z.string().optional()
};
const validateProcessSpec = (value, ctx) => {
	try {
		const { command, environment, cwd } = value;
		require_validation.normalizeSandboxProcessStartOptions({
			command,
			...environment !== void 0 && { environment },
			...cwd !== void 0 && { cwd }
		});
	} catch (error) {
		ctx.addIssue({
			code: zod_v3.z.ZodIssueCode.custom,
			message: error instanceof Error ? error.message : "invalid process spec"
		});
	}
};
const processSpecInputSchema = zod_v3.z.object(processSpecShape).strict().superRefine(validateProcessSpec);
const execInputSchema = zod_v3.z.object({
	...processSpecShape,
	timeoutMs: zod_v3.z.number().int().positive().max(3e5)
}).strict().superRefine(validateProcessSpec);
const signalInputSchema = zod_v3.z.object({
	signal: zod_v3.z.number().int().min(1).max(64),
	includeChildren: zod_v3.z.boolean()
}).strict();
const waitInputSchema = zod_v3.z.object({ timeoutMs: zod_v3.z.number().int().positive().max(3e5) }).strict();
const outputInputSchema = zod_v3.z.object({ tailBytes: zod_v3.z.number().int().min(0).max(512 * 1024) }).strict();
const maxDurableSandboxExecOutputBytes = 2 << 20;
const operationBase = { protocolVersion: zod_v3.z.literal(require_types.sandboxProtocolVersion) };
const sandboxOperationSchema = zod_v3.z.discriminatedUnion("action", [
	zod_v3.z.object({
		...operationBase,
		action: zod_v3.z.literal("create"),
		input: zod_v3.z.tuple([createInputSchema])
	}).strict(),
	zod_v3.z.object({
		...operationBase,
		action: zod_v3.z.literal("list"),
		input: zod_v3.z.tuple([listInputSchema])
	}).strict(),
	zod_v3.z.object({
		...operationBase,
		action: zod_v3.z.literal("get"),
		input: zod_v3.z.tuple([getInputSchema])
	}).strict(),
	zod_v3.z.object({
		...operationBase,
		action: zod_v3.z.literal("waitUntilRunning"),
		target: sandboxTargetSchema,
		input: zod_v3.z.tuple([waitInputSchema])
	}).strict(),
	zod_v3.z.object({
		...operationBase,
		action: zod_v3.z.literal("exec"),
		target: sandboxTargetSchema,
		input: zod_v3.z.tuple([execInputSchema])
	}).strict(),
	zod_v3.z.object({
		...operationBase,
		action: zod_v3.z.literal("destroy"),
		target: sandboxTargetSchema,
		input: zod_v3.z.tuple([])
	}).strict(),
	zod_v3.z.object({
		...operationBase,
		action: zod_v3.z.literal("process.start"),
		target: sandboxTargetSchema,
		input: zod_v3.z.tuple([processSpecInputSchema])
	}).strict(),
	zod_v3.z.object({
		...operationBase,
		action: zod_v3.z.literal("process.list"),
		target: sandboxTargetSchema,
		input: zod_v3.z.tuple([listInputSchema])
	}).strict(),
	zod_v3.z.object({
		...operationBase,
		action: zod_v3.z.literal("process.get"),
		target: processIdTargetSchema,
		input: zod_v3.z.tuple([])
	}).strict(),
	zod_v3.z.object({
		...operationBase,
		action: zod_v3.z.literal("process.signal"),
		target: processTargetSchema,
		input: zod_v3.z.tuple([signalInputSchema])
	}).strict(),
	zod_v3.z.object({
		...operationBase,
		action: zod_v3.z.literal("process.wait"),
		target: processTargetSchema,
		input: zod_v3.z.tuple([waitInputSchema])
	}).strict(),
	zod_v3.z.object({
		...operationBase,
		action: zod_v3.z.literal("process.output"),
		target: processTargetSchema,
		input: zod_v3.z.tuple([outputInputSchema])
	}).strict()
]);
const pageSchema = zod_v3.z.object({
	cursor: zod_v3.z.string().min(1).optional(),
	hasMore: zod_v3.z.boolean(),
	limit: zod_v3.z.number().int().min(1).max(250)
}).strict();
const commandOutputByteCountsSchema = zod_v3.z.object({
	stdout: zod_v3.z.number().int().nonnegative().safe(),
	stderr: zod_v3.z.number().int().nonnegative().safe()
}).strict();
const commandOutputMetadataSchema = zod_v3.z.discriminatedUnion("truncated", [zod_v3.z.object({ truncated: zod_v3.z.literal(false) }).strict(), zod_v3.z.object({
	truncated: zod_v3.z.literal(true),
	strategy: zod_v3.z.literal("tail"),
	originalBytes: commandOutputByteCountsSchema,
	retainedBytes: commandOutputByteCountsSchema
}).strict()]);
const wireCommandResultSchema = zod_v3.z.object({
	stdout: zod_v3.z.string(),
	stderr: zod_v3.z.string(),
	encoding: zod_v3.z.literal("base64"),
	exitCode: zod_v3.z.number().int().min(-2147483648).max(2147483647),
	output: commandOutputMetadataSchema
}).strict().superRefine((result, ctx) => {
	if (!result.output.truncated) return;
	const { originalBytes, retainedBytes } = result.output;
	if (retainedBytes.stdout > originalBytes.stdout || retainedBytes.stderr > originalBytes.stderr) ctx.addIssue({
		code: zod_v3.z.ZodIssueCode.custom,
		message: "retained output bytes cannot exceed original output bytes",
		path: ["output", "retainedBytes"]
	});
	const originalTotal = originalBytes.stdout + originalBytes.stderr;
	const retainedTotal = retainedBytes.stdout + retainedBytes.stderr;
	if (retainedTotal >= originalTotal) ctx.addIssue({
		code: zod_v3.z.ZodIssueCode.custom,
		message: "truncated output must retain fewer bytes than the original",
		path: ["output"]
	});
	if (retainedTotal > maxDurableSandboxExecOutputBytes) ctx.addIssue({
		code: zod_v3.z.ZodIssueCode.custom,
		message: "retained output exceeds the durable sandbox exec limit",
		path: ["output", "retainedBytes"]
	});
});
const wireDestroyResultSchema = zod_v3.z.discriminatedUnion("status", [zod_v3.z.object({
	status: zod_v3.z.literal("TERMINATING"),
	sandbox: require_validation.sandboxRefSchema.refine((sandbox) => sandbox.status === "TERMINATING", "destroyed sandbox must be TERMINATING")
}).strict(), zod_v3.z.object({
	status: zod_v3.z.literal("TERMINATED"),
	sandbox: zod_v3.z.null()
}).strict()]);
const wireWaitProcessSchema = zod_v3.z.object({
	kind: zod_v3.z.literal("inngest/sandbox.process"),
	version: zod_v3.z.literal(1),
	sandboxId: require_validation.canonicalUuidSchema,
	id: require_validation.canonicalUuidSchema,
	state: zod_v3.z.enum([
		"EXITED",
		"KILLED",
		"FAILED",
		"LOST"
	]),
	exitCode: zod_v3.z.number().int().optional(),
	terminationSignal: zod_v3.z.number().int().min(1).max(64).optional()
}).strict().superRefine((process, ctx) => {
	if (process.state === "EXITED" !== (process.exitCode !== void 0)) ctx.addIssue({
		code: zod_v3.z.ZodIssueCode.custom,
		message: "exitCode must be present only for EXITED processes"
	});
	if (process.state === "KILLED" !== (process.terminationSignal !== void 0)) ctx.addIssue({
		code: zod_v3.z.ZodIssueCode.custom,
		message: "terminationSignal must be present only for KILLED processes"
	});
});
const sandboxOperationResultSchema = zod_v3.z.discriminatedUnion("action", [
	zod_v3.z.object({
		...operationBase,
		action: zod_v3.z.literal("create"),
		sandbox: require_validation.sandboxRefSchema
	}).strict(),
	zod_v3.z.object({
		...operationBase,
		action: zod_v3.z.literal("list"),
		sandboxes: zod_v3.z.array(require_validation.sandboxRefSchema),
		page: pageSchema,
		fetchedAt: zod_v3.z.string().min(1)
	}).strict(),
	zod_v3.z.object({
		...operationBase,
		action: zod_v3.z.literal("get"),
		sandbox: require_validation.sandboxRefSchema.nullable()
	}).strict(),
	zod_v3.z.object({
		...operationBase,
		action: zod_v3.z.literal("waitUntilRunning"),
		sandbox: require_validation.sandboxRefSchema.refine((sandbox) => sandbox.status === "RUNNING", "waitUntilRunning sandbox must be RUNNING")
	}).strict(),
	zod_v3.z.object({
		...operationBase,
		action: zod_v3.z.literal("exec"),
		result: wireCommandResultSchema
	}).strict(),
	zod_v3.z.object({
		...operationBase,
		action: zod_v3.z.literal("destroy"),
		result: wireDestroyResultSchema
	}).strict(),
	zod_v3.z.object({
		...operationBase,
		action: zod_v3.z.literal("process.start"),
		process: require_validation.sandboxProcessRefSchema
	}).strict(),
	zod_v3.z.object({
		...operationBase,
		action: zod_v3.z.literal("process.list"),
		processes: zod_v3.z.array(require_validation.sandboxProcessRefSchema),
		page: pageSchema,
		fetchedAt: zod_v3.z.string().min(1)
	}).strict(),
	zod_v3.z.object({
		...operationBase,
		action: zod_v3.z.literal("process.get"),
		process: require_validation.sandboxProcessRefSchema.nullable()
	}).strict(),
	zod_v3.z.object({
		...operationBase,
		action: zod_v3.z.literal("process.signal"),
		result: zod_v3.z.null()
	}).strict(),
	zod_v3.z.object({
		...operationBase,
		action: zod_v3.z.literal("process.wait"),
		process: wireWaitProcessSchema
	}).strict(),
	zod_v3.z.object({
		...operationBase,
		action: zod_v3.z.literal("process.output"),
		result: zod_v3.z.object({ chunks: zod_v3.z.array(require_validation.wireOutputChunkSchema) }).strict()
	}).strict()
]);
const sandboxErrorPayloadSchema = zod_v3.z.object({
	protocolVersion: zod_v3.z.literal(require_types.sandboxProtocolVersion),
	action: zod_v3.z.enum([
		"create",
		"list",
		"get",
		"waitUntilRunning",
		"exec",
		"destroy",
		"process.start",
		"process.list",
		"process.get",
		"process.signal",
		"process.wait",
		"process.output"
	]),
	code: zod_v3.z.string().min(1),
	message: zod_v3.z.string().min(1),
	status: zod_v3.z.number().int().min(100).max(599).optional(),
	sandboxId: require_validation.canonicalUuidSchema.optional(),
	processId: require_validation.canonicalUuidSchema.optional(),
	ambiguous: zod_v3.z.boolean(),
	retryable: zod_v3.z.boolean(),
	requestId: zod_v3.z.string().min(1).optional(),
	details: zod_v3.z.array(zod_v3.z.record(zod_v3.z.unknown())).default([])
}).passthrough();
const sandboxValidationErrorPayloadSchema = zod_v3.z.object({
	protocolVersion: zod_v3.z.literal(require_types.sandboxProtocolVersion),
	type: zod_v3.z.literal("sandbox_validation_error"),
	message: zod_v3.z.string().min(1)
}).passthrough();
const parseSandboxOperation = (value) => require_validation.parseWithSchema(sandboxOperationSchema, value, "sandbox operation");
const parseSandboxOperationForAction = (action, value) => {
	const operation = parseSandboxOperation(value);
	if (operation.action !== action) throw new require_types.SandboxValidationError(`Expected sandbox ${action} operation, received ${operation.action}`);
	return operation;
};
const findSandboxErrorOptions = (error) => {
	let current = error;
	for (let depth = 0; depth < 6 && current; depth++) {
		const parsed = sandboxErrorPayloadSchema.safeParse(current);
		if (parsed.success) return {
			...parsed.data,
			code: parsed.data.code,
			details: parsed.data.details,
			cause: error
		};
		if (typeof current !== "object" || !("cause" in current)) return;
		current = current.cause;
	}
};
const findSandboxValidationError = (error) => {
	let current = error;
	for (let depth = 0; depth < 6 && current; depth++) {
		const parsed = sandboxValidationErrorPayloadSchema.safeParse(current);
		if (parsed.success) return { message: parsed.data.message };
		if (typeof current !== "object" || !("cause" in current)) return;
		current = current.cause;
	}
};
const operationSandboxId = (operation) => {
	if (operation.action === "get") return operation.input[0].sandboxId;
	if ("target" in operation) return operation.target.sandbox.id;
};
const operationProcessId = (operation) => "target" in operation ? "processId" in operation.target ? operation.target.processId : "process" in operation.target ? operation.target.process.id : void 0 : void 0;
const validateSandboxResult = (operation, rawResult) => {
	const result = require_validation.parseWithSchema(sandboxOperationResultSchema, rawResult, "sandbox operation result");
	if (result.action !== operation.action) throw new require_types.SandboxValidationError(`Sandbox operation returned ${result.action} for ${operation.action}`);
	const sandboxId = operationSandboxId(operation);
	const processId = operationProcessId(operation);
	if (sandboxId && "sandbox" in result && result.sandbox && result.sandbox.id !== sandboxId) throw new require_types.SandboxValidationError("Sandbox operation returned an unrelated sandbox");
	if (sandboxId && "process" in result && result.process && "sandboxId" in result.process && result.process.sandboxId !== sandboxId) throw new require_types.SandboxValidationError("Sandbox operation returned a process from an unrelated sandbox");
	if (processId && "process" in result && result.process && "id" in result.process && result.process.id !== processId) throw new require_types.SandboxValidationError("Sandbox operation returned an unrelated process");
	return result;
};

//#endregion
exports.findSandboxErrorOptions = findSandboxErrorOptions;
exports.findSandboxValidationError = findSandboxValidationError;
exports.maxDurableSandboxExecOutputBytes = maxDurableSandboxExecOutputBytes;
exports.parseSandboxOperation = parseSandboxOperation;
exports.parseSandboxOperationForAction = parseSandboxOperationForAction;
exports.validateSandboxResult = validateSandboxResult;
//# sourceMappingURL=protocol.cjs.map