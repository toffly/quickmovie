const require_NonRetriableError = require('../NonRetriableError.cjs');
const require_middleware = require('../middleware/middleware.cjs');
const require_types = require('./types.cjs');
const require_durable = require('./durable.cjs');

//#region src/components/sandbox/middleware.ts
const executeAsStep = async (client, operation) => {
	try {
		return await require_durable.executeSandboxOperation(client.sandboxes, operation);
	} catch (error) {
		if (error instanceof require_types.SandboxError) {
			const cause = {
				protocolVersion: error.protocolVersion,
				action: error.action,
				code: error.code,
				message: error.message,
				...error.status !== void 0 && { status: error.status },
				...error.sandboxId !== void 0 && { sandboxId: error.sandboxId },
				...error.processId !== void 0 && { processId: error.processId },
				ambiguous: error.ambiguous,
				retryable: error.retryable,
				...error.requestId !== void 0 && { requestId: error.requestId },
				details: [...error.details]
			};
			if (error.retryable) {
				const retryableError = new Error(error.message, { cause });
				retryableError.name = error.name;
				throw retryableError;
			}
			throw new require_NonRetriableError.NonRetriableError(error.message, { cause });
		}
		if (error instanceof require_types.SandboxValidationError) throw new require_NonRetriableError.NonRetriableError(error.message, { cause: {
			protocolVersion: require_types.sandboxProtocolVersion,
			type: "sandbox_validation_error",
			message: error.message
		} });
		throw error;
	}
};
/**
* Adds the durable `step.sandbox` facade using ordinary `step.run` calls.
*
* The executor only sees a normal planned step. Its handler calls the same REST
* client exposed as `inngest.sandboxes`, then returns JSON-safe wire data for
* replay.
*/
var SandboxMiddleware = class extends require_middleware.Middleware.BaseMiddleware {
	id = "inngest:sandbox";
	transformFunctionInput(arg) {
		const rawTool = (idOrOptions, operation) => arg.ctx.step.run(idOrOptions, (input) => executeAsStep(this.client, input), operation);
		return {
			...arg,
			ctx: {
				...arg.ctx,
				step: {
					...arg.ctx.step,
					sandbox: require_durable.createSandboxTools(() => rawTool)
				}
			}
		};
	}
};
/**
* EXPERIMENTAL: This API is not yet stable and may change in the future without
* a major version bump.
*
* Middleware that enables the experimental durable `step.sandbox` API.
*
* The direct `inngest.sandboxes` client does not require this middleware.
*
* @example
* ```ts
* import { sandboxMiddleware } from "inngest/experimental";
*
* const inngest = new Inngest({
*   id: "my-app",
*   middleware: [sandboxMiddleware()],
* });
* ```
*/
const sandboxMiddleware = () => SandboxMiddleware;

//#endregion
exports.sandboxMiddleware = sandboxMiddleware;
//# sourceMappingURL=middleware.cjs.map